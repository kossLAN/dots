import QtQuick
import qs

Rectangle {
    id: root
    radius: 12
    color: ShellSettings.colors.active.window

    border {
        width: 1
        color: ShellSettings.colors.active.light
    }
}
